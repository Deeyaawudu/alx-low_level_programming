#include	<stdio.h>

/**
 * main	-	Prints	the	alphabet	without	q	ande
 *
 * Return:	Always	0
*/

int	main(void)
{
	int	i;
		for	(i	=	97;	i	<	123;	i++)

		putchar(i	!=	101	&&	i	!=	113);
		
			putchar(i);
	putchar('\n');
	return	(0);
}
